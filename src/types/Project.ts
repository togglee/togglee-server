export type DBProject = {
  name: string;
  id: string;
  owner: string;
  toggles: string;
  isTest: number;
};

export type Project = {
  name: string;
  id: string;
  owner: string;
  toggles: string;
  url: string;
  isTest: number;
};
